{% comment %}
Instantiate with a palette source; e.g.:

```sh
cargo run -p base16cs-cli -- \
    --palette base16cs/colorschemes/selenized_light.yaml \
    --template ~/Code/pensieve/_data/colorscheme.yml.liquid \
  > ~/Code/pensieve/_data/colorschemes/selenized_light.yml
```
{% endcomment %}

palette: {{ palette.name }}
colors:
{% for color in palette.colors %}
  - name: {{ color.base.name }}
    lab:
      l: {{ color.base.lab.l }}
      a: {{ color.base.lab.a }}
      b: {{ color.base.lab.b }}
    rgb:
      r: {{ color.rgb.r | round }}
      g: {{ color.rgb.g | round }}
      b: {{ color.rgb.b | round }}
      hex: "{{ color.rgb_hex }}"
{% endfor %}
